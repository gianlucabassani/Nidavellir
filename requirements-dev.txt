# Development & CI dependencies.
# Install with:  pip install -r requirements-dev.txt
# (also installs the runtime deps the tests import)

-r cyber-range/services/scenario-orchestrator/requirements.txt

# Test runner
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27          # required by fastapi.testclient

# Lint / format / static checks
ruff>=0.6
bandit>=1.7          # security linter for Python
