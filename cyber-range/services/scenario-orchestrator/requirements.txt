# Core dependencies - Minimal Phase 1
pyyaml>=6.0
python-dotenv>=0.21.0
requests>=2.28.0
openstacksdk>=1.0.0
dotenv

# Web API (Phase 2 - optional)
fastapi>=0.115.0,<1.0
# Starlette 1.x switches TestClient to the experimental httpx2 stack.  Keep the
# API/runtime on the mature ASGI compatibility line until FastAPI 1.x is adopted
# and exercised deliberately.
starlette>=0.40.0,<1.0
uvicorn[standard]>=0.21.0
pydantic>=2.0.0
python-multipart>=0.0.9
flask
bootstrap-flask

# new requirements 

celery>=5.3.0
redis>=5.0.0
# Worker-side scripted trial driver talks to the existing scoped MCP gateway.
mcp>=1.2,<2.0


# API rate limiting (Phase 1 hardening)
slowapi>=0.1.9

# docker-local provider (ADR-0003)
docker>=7.0.0

# Persistence layer (ADR-0004): SQLAlchemy ORM + Alembic migrations.
# SQLite (stdlib) stays the zero-dependency default; psycopg is only
# exercised when DATABASE_URL points at PostgreSQL.
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1

# Secrets hygiene (audit #14): Fernet encryption of lab outputs at rest.
cryptography>=42.0
